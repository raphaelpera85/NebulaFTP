# Pinned via upper bounds known-good as of 2026-07.
# Re-pin with: pip-compile --upgrade requirements.in
# Verify with: pip install --require-hashes -r requirements.lock
# (commit requirements.lock for reproducible installs).

bcrypt>=4.2,<5
cryptography>=43,<46
dnspython>=2.6,<3
motor>=3.6,<4
pyftpdlib>=2.0,<3
pyrofork>=2.3.69,<3
pymongo>=4.8,<5
python-dotenv>=1.0,<2
aiofiles>=24,<26
tgcrypto>=1.2.5,<2

# Runtime framework pieces (often left transitive but pin for clarity).
aiohttp>=3.10,<4
pyaes>=1.6,<2
pysocks>=1.7,<2
pystray>=0.19,<1
Pillow>=10,<13
